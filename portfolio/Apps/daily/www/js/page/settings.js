/**
 * Page - Settings
 */

(function() {'use strict';

	var page = app.registerPage('page-settings', {

		// initialize page layouts
		onPageInit: function() {
		},

		// resize all elements according to the screen
		onResize: function() {
		},

		// called when the page becomes visible
		onPageShow: function() {
			this.onResize();
		},

		// called when the page becomes invisible
		onPageHide: function() {
		}
	});

})();

